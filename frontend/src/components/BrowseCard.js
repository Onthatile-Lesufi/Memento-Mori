import { Col } from "react-bootstrap";
import { Link } from "react-router-dom";

const BrowseCard = ({name, image, link}) => {
    return (
        <Link to={link}>
            <Col>

            </Col>
        </Link>
    )
}

export default BrowseCard;